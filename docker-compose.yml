
services:
  app:
    platform: linux/amd64 
    build: .
    container_name: legal_ai_app
    restart: "no"
    env_file:
      - .env
    depends_on:
      - db
    volumes:
      # Mount data so you can update json/npy files without rebuilding the image
      - ./data:/app/data
    ports:
      - "8000:8000"   # host:container
    networks:
      - legal_net

  db:
    image: postgres:16
    container_name: legal_ai_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-legal_ai}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
      POSTGRES_DB: ${DB_NAME:-legal_ai}
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - legal_net

  # Optional: pgAdmin UI to inspect DB
  pgadmin:
    image: dpage/pgadmin4
    container_name: legal_pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      - db
    networks:
      - legal_net

networks:
  legal_net:

volumes:
  db_data:
